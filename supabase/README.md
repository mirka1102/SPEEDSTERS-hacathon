# supabase/ — Altair's schema

Not created yet — write `schema.sql` here with the `programs` and `profiles` tables from
`docs/SPEC.md` §4 (includes the §10 additions: `programs.image_url`, `programs.campus_life_note`,
`profiles.favorites`). Seed `programs` from `apps/api/data/programs.json` once it's ready.
