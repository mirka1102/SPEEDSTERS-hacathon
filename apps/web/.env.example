# URL of a running apps/api instance. Leave unset to run entirely against the built-in mock
# engine (apps/web/lib/mockEngine.ts) — every screen falls back to mock data automatically if a
# real-backend call fails, so the app never has to know or care which mode it's in.
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000
